[package]
name = "minio-store"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1.6.0"
futures = "0.3.30"
iroh-blobs = { version = "0.20.0", features = ["downloader"], default-features = false }
